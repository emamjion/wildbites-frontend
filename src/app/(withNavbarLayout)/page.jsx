import HeroSection from "@/components/HeroSection";

const HomePage = () => {
    return (
        <div>
            {/* Hero Section */}
            <HeroSection/>
        </div>
    );
};

export default HomePage;